[upx](https://upx.github.io) is a binary packer.

# What's a packer?
It's a compressor that takes in an binary/executable compresses it and creates a new executable that decompresses and executes on the fly.
